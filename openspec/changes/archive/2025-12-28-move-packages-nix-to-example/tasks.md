## 1. Implementation

- [x] 1.1 Create `example/packages.nix` as a template with commented examples
- [x] 1.2 Remove existing root `packages.nix` from upstream git tracking
- [x] 1.3 Update README.md with instructions for copying example to root

## 2. Validation

- [x] 2.1 Verify `nix build` works after copying example to root
- [x] 2.2 Verify existing tests pass
